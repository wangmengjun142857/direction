  //定义变量，有没有var有区别吗？（已解决）
  
  //要创建一个运行于无穷循环中的计数器，我们需要编写一个函数来调用其自身。在下面的代码，当按钮被点击后，输入域便从0开始计数。
  //困惑：怎么添加一个按钮，让其停止计数。 (已解决)
  
  //先建一个blog文件夹的话，再按照步骤走的话会变成在blog文件夹里再建一个blog文件夹，不知道怎么回事;但如果不建blog文件夹时，会提示错误。（git 命令）
  //hexo d 上传失败；域名网站无法打开。 （已解决）

