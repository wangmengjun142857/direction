switch语句中，内部结构为 case值： 

setTimeout("hideBox()", 5000);
setTimeout(hideBox(), 5000);
这两者的区别是第一个会延迟5秒执行,第二个会立即执行. 结论,方法一定要用引号。

在函数作用域内 加 var 定义的变量是局部变量，不加 var 定义的就成了(隐式)全局变量。
在全局作用域下，使用 var 定义的变量不可以 delete，没有 var 定义的变量可以 delete。也就说明
      隐式全局变量严格来说不是真正的变量，而是全局对象的属性，因为属性可以通过 delete 删除，而变量不可以。
使用 var 定义变量还会提升变量声明。
